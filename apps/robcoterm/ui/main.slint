// Placeholder UI. Real overview/detail pages land in Phase 3
// (quarters.slint, lights.slint, ...). This only exists so slint-build has
// something to compile during the Phase 0 spike.
export component App inherits Window {
    Text {
        text: "hi";
    }
}
